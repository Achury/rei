$(function() {
  if ($("#messages[data-url]").size() > 0) {
    $("#messages[data-url]").find(".spinner").slideDown();
    setTimeout(function(){ $.getScript( $("#messages[data-url]").attr("data-url") ); }, 1);
  }
  if ($("#homework[data-url]").size() > 0) {
    $("#homework[data-url]").find(".spinner").slideDown();
    setTimeout(function(){ $.getScript( $("#homework[data-url]").attr("data-url") ); }, 1);
  }
  if ($("#resources[data-url]").size() > 0) {
    $("#resources[data-url]").find(".spinner").slideDown();
    setTimeout(function(){ $.getScript( $("#resources[data-url]").attr("data-url") ); }, 1);
  }
});